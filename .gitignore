# dependencies
node_modules/

# Next.js
.next/
out/

# env files
.env
.env.local
.env.*.local

# OS
.DS_Store

# build
dist/
output/

# large source data files (Turso is the source of truth)
data/*.csv
data/*.db
data/*.db-shm
data/*.db-wal
data/*.xlsx
# Exception: the CDM Request form template ships with the build because
# /api/cdm-request/[ndc]?format=xlsx reads it at runtime to produce the
# autofilled download. Keep this file in the repo so the Vercel deploy
# includes it (combined with outputFileTracingIncludes in next.config.mjs).
!data/cdm_request_template.xlsx
data/*.zip
data/*.json
data/_test_write
data/snapshots/

# Next.js / TypeScript build artifacts
tsconfig.tsbuildinfo

.vercel
.env*.local
